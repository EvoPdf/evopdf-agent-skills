# Security

This repository contains documentation only. If you believe a skill leads assistants to insecure code (for example unsafe defaults for `LocalFilesEnabled`, `AllowInsecureContent` or `IgnoreCertificateErrors`), open an issue or write to support@evopdf.com.

Security issues in the EvoPdf Next library itself: support@evopdf.com.
